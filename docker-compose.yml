services:
  bot:
    build: .
    image: discord-file-search-bot
    container_name: discord-file-search-bot
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/files.db
    volumes:
      - ./data:/data
